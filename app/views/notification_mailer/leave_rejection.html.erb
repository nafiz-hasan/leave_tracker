<!DOCTYPE html>
<html>
<head>
  <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
</head>
<body>
<h2>Dear, <%= @user.name %></h2>
<h2>Your Request for leave: </h2><hr/>

<table border="2px">
  <thead>
  <tr>
    <th>Date</th>
    <th>Duration</th>
  </tr>
  </thead>
  <tbody>
  <% @days.each do |d| %>
      <tr>
        <td><%= d.the_date.strftime("%a, %d %b, %Y") %></td>
        <td><%= number_to_human(d.hours) + " Hours" %></td>
      </tr>
  <% end %>

  </tbody>
</table>

<h1>Is Rejected</h1><hr/>

<h3>Feedback from your TTF</h3>
<p><%= @feedback %></p><hr/>

<%= holiday_url(@holiday, host: 'localhost:3000') %>

</body>
</html>